# FlatShare

This is a web app to help flat members split bills easily.

## How to run it locally

1. Clone this repository
2. Install the dependencies by `pip install -r requirements.txt`
3. Run the app by `python __init__.py`
